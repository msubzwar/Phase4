<%= simple_form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<table border="0" cellspacing="5" cellpadding="5">
		<tr valign="top">
			<td>
				<h2>Job Details:</h2>
				
				<div class="input-field">
			    <%= f.label :Name %>
			    <%= f.text_field :name %>
			  </div>
			  
			  <div class="input-field">
			    <%= f.label :Description %>
			    <%= f.text_field :description %>
			  </div>
	
	<!-- radion button needs to be added-->

             <div class="field">
 
                <%= f.input :active %>
            </div>
             
</br>
</br></br>
</br></br>
			  <div class="actions">
                <%= f.submit :class=>"btn waves-effect waves-light" %>
                <%= link_to 'Cancel', shifts_path, :class => 'waves-effect waves-light btn red' %>
              </div>
			</td>
			<td width="25">&nbsp;</td>
		</tr>
	</table>
<% end %>
