<%= form_for(@pay_grade) do |f| %>
  <% if @pay_grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pay_grade.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @pay_grade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<table border="0" cellspacing="5" cellpadding="5">
		<tr valign="top">
			<td>
				<h2>Pay Grade Details:</h2>
				
			 <div class="input-field">
	            <%= f.label :Level %>
                <%= f.text_field :level %>
			  </div>
			  <!-- need to add checkboxx -->
			  <div class="input-field">
	            <%= f.label :Active %>
                <%= f.text_field :active %>
			  </div>

</br>
</br></br>
</br></br>
			  <div class="actions">
                <%= f.submit :class=>"btn waves-effect waves-light" %>
                <%= link_to 'Cancel', pay_grades_path, :class => 'waves-effect waves-light btn red' %>
              </div>
			</td>
			<td width="25">&nbsp;</td>
		</tr>
	</table>
<% end %>
